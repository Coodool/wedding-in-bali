<!--pages/guest/guest.wxml-->
<view class="container" bindtap="clickBG" style="opacity: {{opacity / 100}}">
  <view wx:if="{{!hasUserInfo}}">
    <view class="btn-wrapper">
      <button class="btn" open-type="getUserInfo"
  bindgetuserinfo="bindGetUserInfo">点击进入</button>
    </view>
  </view>
</view>
